objInspect operator:
. add ini file w. location of ezpo docs (online or local repos)
. add ini file w. version of yui to use, test using 2.8.1
. support using jsonrpc instead of jquery if ezjscore is not installed but ggwebservices is
. show a friendly error msg if none of the two is loaded (can be tested in tpl code)
. show a loading icon while ajax calls are executing (use yui dynamic tree)
. remove unused images and corresponding styles in css
. make js loading more friendly to yui:
  if ezjscore is found, leave it up to yui to load the js,
  otherwise do it by ourselves (need the 2-in-3 yui hacks)
